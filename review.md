# what does this file do, what would I change, what don't I fully understand

# IN types.py we have defined the classes that we can further use in tools.py and orchestrator and we have defined those classes which have multiple attributes , like employmenttype has 3 attributes either SALARIED, GIG, SELF employed. 

# Somewhere we have also defined the attributes of the entire loan application and the decision here, there are also certain wrappers used in here to wrap the entire class 

# The Python concepts used here are quiet confusing for me

# run.py:

# In orchestrator after defining the sequence then we have done the we are creating a sample loan application in run.py and will run it here and so we are calling the function runagent() here as well

# I wont change anything for now 
# The underlying python concepts

# Orchestrator.py:
 
 # In this file we have defined the sequence in which functions would be called

 # If i want to remove a function from here which is unnecessary i would remove it straight it from here and so on

 # I didnt understood is ctx an object or an class or a variable here in python and how it is used so far surprises me 

 # types.py:

 # IN this file we are trying to generate random data without using APIS by using some built in pythonfunctions we already had defined the structure of the five functions in tools.py we are using that structure to define the constraints here in this file

 # No i wont change anything here

 # The Python concepts are still quiet confusing here and the thought process of how it was built and what was the source of building it 